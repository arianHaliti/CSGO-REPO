import React, { Fragment } from "react";
import SearchBar from "../../components/layout/SearchBar";
const Home = () => {
  return (
    <Fragment>
      <SearchBar />
    </Fragment>
  );
};

export default Home;
